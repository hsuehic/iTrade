# iTrade Console Environment Configuration Template
# 
# 使用方法：
# 1. 复制此文件为 .env：cp env.template .env
# 2. 编辑 .env 文件，替换占位符为真实值
# 3. 确保 .env 文件不被提交到版本控制

# =====================================
# 数据库配置
# =====================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_DB=itrade
DB_SSL=false


# =====================================
# 账户/交易所账号/策略同步轮询配置
# =====================================
BOT_REFRESH_INTERVAL_MS=60000 # bot(用户)刷新(交易所账号)间隔（毫秒），默认60秒

ACCOUNT_POLLING_INTERVAL=60000 # 交易所账号轮询间隔（毫秒），默认60秒
STRATEGY_SYNC_INTERVAL_MS=60000 # 策略同步间隔（毫秒），默认60秒

# =====================================
# 使用说明
# =====================================
# 1. 请将上面的占位符替换为您的真实 API 密钥
# 2. 确保 API 密钥有足够的权限（至少需要读取账户信息的权限）
# 3. 如果使用测试网，请在对应交易所的测试网站获取测试 API 密钥
# 4. 不需要的交易所可以注释掉或删除对应的配置行

# =====================================
# 安全提醒
# =====================================
# - 这个文件包含敏感信息，请不要提交到版本控制
# - 确保 .env 文件在 .gitignore 中
# - 生产环境中使用环境变量而不是 .env 文件

ENCRYPTION_KEY=your_encryption_key_here
OPEN_ORDERS_SYNC_INTERVAL=numberofmilliseconds (minimum 1000, suggest 60000)

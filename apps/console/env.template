# iTrade Console Environment Configuration Template
# 
# 使用方法：
# 1. 复制此文件为 .env：cp env.template .env
# 2. 编辑 .env 文件，替换占位符为真实值
# 3. 确保 .env 文件不被提交到版本控制

# =====================================
# 数据库配置
# =====================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_DB=itrade
DB_SSL=false

# =====================================
# 交易所 API 配置
# =====================================

# Binance 交易所
# 获取方式: https://www.binance.com/cn/my/settings/api-management
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET_KEY=your_binance_secret_key_here

# OKX 交易所 (可选)
# 获取方式: https://www.okx.com/account/my-api
# OKX_API_KEY=your_okx_api_key_here
# OKX_SECRET_KEY=your_okx_secret_key_here
# OKX_PASSPHRASE=your_okx_passphrase_here

# Coinbase 交易所 (可选)
# 获取方式: https://pro.coinbase.com/profile/api
# COINBASE_API_KEY=your_coinbase_api_key_here
# COINBASE_SECRET_KEY=your_coinbase_secret_key_here

# Coinbase 账户类型配置
# - all (默认): 返回 Spot + INTX 余额
# - intx: 仅返回 INTX 永续合约余额 (USDC)
# - spot: 仅返回现货余额
# COINBASE_ACCOUNT_TYPE=all

# =====================================
# 账户轮询配置
# =====================================
# 轮询间隔（毫秒），默认60秒
ACCOUNT_POLLING_INTERVAL=60000

# =====================================
# 使用说明
# =====================================
# 1. 请将上面的占位符替换为您的真实 API 密钥
# 2. 确保 API 密钥有足够的权限（至少需要读取账户信息的权限）
# 3. 如果使用测试网，请在对应交易所的测试网站获取测试 API 密钥
# 4. 不需要的交易所可以注释掉或删除对应的配置行

# =====================================
# 安全提醒
# =====================================
# - 这个文件包含敏感信息，请不要提交到版本控制
# - 确保 .env 文件在 .gitignore 中
# - 生产环境中使用环境变量而不是 .env 文件

# Web Dashboard Configuration Template
# 
# 使用方法：
# 1. 复制此文件为 .env.local：cp env.local.template .env.local
# 2. 根据需要修改配置值

# Dashboard refresh interval (milliseconds)
# 与轮询服务的6秒同步，设置为6秒刷新
NEXT_PUBLIC_DASHBOARD_REFRESH_INTERVAL=6000

# Database configuration (if needed for API routes)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_DB=itrade
DB_SSL=false

{
  "name": "@crypto-trading/utils",
  "version": "0.1.0",
  "description": "Utility functions and helpers for crypto trading",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown src/index.ts --format cjs && tsdown src/index.ts --format esm && tsc --project tsconfig.build.json && cp dist/utils/src/index.d.ts dist/ && cp dist/utils/src/index.d.ts.map dist/ 2>/dev/null || true",
    "dev": "tsdown src/index.ts --format esm --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "decimal.js": "^10.4.3",
    "moment": "^2.30.1",
    "moment-timezone": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "tsdown": "^0.2.16"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}

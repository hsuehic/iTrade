# =============================================================
# GCE Connection Info — for db-sync.sh
# =============================================================
# Copy to project root: cp deploy/env.gce.template .env.gce
# .env.gce is gitignored — never commit it.
# =============================================================
GCE_HOST=34.x.x.x
GCE_USER=your_gce_username

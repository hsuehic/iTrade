# =============================================================
# iTrade — GitHub Actions CD: Deploy to Google Compute Engine
# =============================================================
#
# Triggers: push to 'main' branch, or manual dispatch
#
# Strategy (near-zero downtime):
#   Job 1 — Build Docker images on CI runner → push to GHCR
#   Job 2 — SSH to GCE → pre-pull images (while old is running)
#            → selectively recreate only changed services
#            → DB and nginx stay running throughout
#
# Downtime minimization:
#   - Images are pre-pulled while old containers still serve traffic
#   - Only console/web/schema-migrator are recreated (not DB or nginx)
#   - Certbot runs conditionally (skipped if cert is valid)
#   - Docker cleanup runs post-deploy (after services are healthy)
#
# Required GitHub Secrets:
#   GCE_HOST            → GCE instance external IP or domain
#   GCE_USER            → SSH username (e.g. your_gce_username)
#   GCE_SSH_PRIVATE_KEY → base64-encoded private key
#
# Required GitHub Permissions:
#   packages: write     → push images to GHCR (auto-granted via GITHUB_TOKEN)
# =============================================================

name: Deploy to GCE

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: 'Force rebuild all Docker images (no cache)'
        required: false
        default: false
        type: boolean

concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io

jobs:
  # ════════════════════════════════════════════════════════════
  # Job 1: Build Docker images on CI runner and push to GHCR
  # ════════════════════════════════════════════════════════════
  build-and-push:
    name: 🔨 Build & Push Images
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compute image metadata
        id: meta
        run: |
          REPO_LC=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          PREFIX="${{ env.REGISTRY }}/${REPO_LC}"
          SHA="${{ github.sha }}"
          echo "prefix=${PREFIX}" >> "$GITHUB_OUTPUT"
          echo "sha=${SHA}" >> "$GITHUB_OUTPUT"
          echo "Image prefix: ${PREFIX}"

      - name: Login to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ${{ env.REGISTRY }} -u ${{ github.actor }} --password-stdin

      # ── Pull previous images for layer cache ────────────────
      - name: Pull previous images for cache
        if: github.event.inputs.force_rebuild != 'true'
        run: |
          PREFIX="${{ steps.meta.outputs.prefix }}"
          docker pull "${PREFIX}/deps:latest" 2>/dev/null && \
            docker tag "${PREFIX}/deps:latest" itrade-deps:latest || true
          docker pull "${PREFIX}/console:latest" 2>/dev/null || true
          docker pull "${PREFIX}/schema-migrator:latest" 2>/dev/null || true
          docker pull "${PREFIX}/web:latest" 2>/dev/null || true

      # ── Build shared dependencies base image ────────────────
      - name: Build deps image
        run: |
          PREFIX="${{ steps.meta.outputs.prefix }}"
          CACHE_FLAG=""
          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ]; then
            CACHE_FLAG="--no-cache"
          else
            CACHE_FLAG="--cache-from itrade-deps:latest"
          fi

          DOCKER_BUILDKIT=1 docker build \
            -f Dockerfile.deps \
            -t itrade-deps:latest \
            -t "${PREFIX}/deps:latest" \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            ${CACHE_FLAG} \
            .

          docker push "${PREFIX}/deps:latest"

      # ── Build and push console image ────────────────────────
      - name: Build and push console image
        run: |
          PREFIX="${{ steps.meta.outputs.prefix }}"
          SHA="${{ steps.meta.outputs.sha }}"
          CACHE_FLAG=""
          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ]; then
            CACHE_FLAG="--no-cache"
          else
            CACHE_FLAG="--cache-from ${PREFIX}/console:latest"
          fi

          DOCKER_BUILDKIT=1 docker build \
            -f apps/console/Dockerfile \
            --target console \
            -t "${PREFIX}/console:latest" \
            -t "${PREFIX}/console:${SHA}" \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            ${CACHE_FLAG} \
            .

          docker push "${PREFIX}/console:latest"
          docker push "${PREFIX}/console:${SHA}"

      # ── Build and push schema-migrator image ────────────────
      - name: Build and push schema-migrator image
        run: |
          PREFIX="${{ steps.meta.outputs.prefix }}"
          SHA="${{ steps.meta.outputs.sha }}"
          CACHE_FLAG=""
          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ]; then
            CACHE_FLAG="--no-cache"
          else
            CACHE_FLAG="--cache-from ${PREFIX}/schema-migrator:latest"
          fi

          DOCKER_BUILDKIT=1 docker build \
            -f apps/console/Dockerfile \
            --target schema-migrator \
            -t "${PREFIX}/schema-migrator:latest" \
            -t "${PREFIX}/schema-migrator:${SHA}" \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            ${CACHE_FLAG} \
            .

          docker push "${PREFIX}/schema-migrator:latest"
          docker push "${PREFIX}/schema-migrator:${SHA}"

      # ── Build and push web image ────────────────────────────
      - name: Build and push web image
        run: |
          PREFIX="${{ steps.meta.outputs.prefix }}"
          SHA="${{ steps.meta.outputs.sha }}"
          CACHE_FLAG=""
          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ]; then
            CACHE_FLAG="--no-cache"
          else
            CACHE_FLAG="--cache-from ${PREFIX}/web:latest"
          fi

          DOCKER_BUILDKIT=1 docker build \
            -f apps/web/Dockerfile \
            -t "${PREFIX}/web:latest" \
            -t "${PREFIX}/web:${SHA}" \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            ${CACHE_FLAG} \
            .

          docker push "${PREFIX}/web:latest"
          docker push "${PREFIX}/web:${SHA}"

  # ════════════════════════════════════════════════════════════
  # Job 2: Pull pre-built images on GCE and restart services
  # ════════════════════════════════════════════════════════════
  deploy:
    name: 🚀 Deploy to GCE
    needs: build-and-push
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH agent
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

          # Decode SSH private key with error handling
          echo "${{ secrets.GCE_SSH_PRIVATE_KEY }}" | base64 -d > ~/.ssh/gce_deploy_key 2>/dev/null || {
            echo "❌ Failed to decode SSH private key"
            echo "The GCE_SSH_PRIVATE_KEY secret may not be properly base64 encoded"
            exit 1
          }

          chmod 600 ~/.ssh/gce_deploy_key

          # Validate SSH key format
          if ! ssh-keygen -l -f ~/.ssh/gce_deploy_key >/dev/null 2>&1; then
            echo "❌ SSH private key format is invalid"
            echo "Please check that GCE_SSH_PRIVATE_KEY is a valid SSH private key"
            exit 1
          fi

          # Start SSH agent
          eval "$(ssh-agent -s)"
          ssh-add ~/.ssh/gce_deploy_key
          echo "SSH_AUTH_SOCK=$SSH_AUTH_SOCK" >> $GITHUB_ENV
          echo "SSH_AGENT_PID=$SSH_AGENT_PID" >> $GITHUB_ENV

          echo "✅ SSH agent configured successfully"

      - name: Add GCE host to known_hosts
        run: |
          echo "🔍 Adding GCE host to known_hosts..."
          echo "Target host: ${{ secrets.GCE_HOST }}"

          # Add host keys to known_hosts with retry
          for attempt in 1 2 3; do
            if ssh-keyscan -H -T 10 "${{ secrets.GCE_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null; then
              echo "✅ Host keys added successfully (attempt $attempt)"
              break
            else
              echo "⚠️  Host key scan failed (attempt $attempt)"
              if [ $attempt -eq 3 ]; then
                echo "❌ All host key scan attempts failed"
                echo "Will attempt connection with StrictHostKeyChecking=no"
              fi
              sleep 2
            fi
          done

      - name: Test SSH connection
        run: |
          echo "🔍 Testing SSH connection..."
          echo "User: ${{ secrets.GCE_USER }}"
          echo "Host: ${{ secrets.GCE_HOST }}"

          # Test with multiple SSH options for robustness
          SSH_OPTS="-o ConnectTimeout=15 -o ServerAliveInterval=10 -o ServerAliveCountMax=3"

          if ssh $SSH_OPTS -o StrictHostKeyChecking=yes "${{ secrets.GCE_USER }}@${{ secrets.GCE_HOST }}" "echo 'SSH connection successful'; whoami; pwd" 2>/dev/null; then
            echo "✅ SSH connection working with StrictHostKeyChecking=yes"
          elif ssh $SSH_OPTS -o StrictHostKeyChecking=no "${{ secrets.GCE_USER }}@${{ secrets.GCE_HOST }}" "echo 'SSH connection successful'; whoami; pwd" 2>/dev/null; then
            echo "✅ SSH connection working with StrictHostKeyChecking=no"
            echo "⚠️  Host key verification was bypassed"
          else
            echo "❌ SSH connection failed with both strict and non-strict host checking"
            echo "Common issues:"
            echo "  - GCE_HOST: Incorrect IP address or hostname"
            echo "  - GCE_USER: Incorrect username"
            echo "  - GCE_SSH_PRIVATE_KEY: Invalid or unauthorized SSH key"
            echo "  - Network: Firewall blocking SSH (port 22)"
            echo "  - Instance: GCE instance not running or accessible"
            exit 1
          fi

      - name: Deploy on GCE
        env:
          GCE_HOST: ${{ secrets.GCE_HOST }}
          GCE_USER: ${{ secrets.GCE_USER }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_ACTOR: ${{ github.actor }}
          COMMIT_SHA: ${{ github.sha }}
          COMMIT_MSG: ${{ github.event.head_commit.message }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          REPO_LC=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          IMAGE_PREFIX="${{ env.REGISTRY }}/${REPO_LC}"

          echo "🚀 Starting zero-downtime deployment to GCE..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "  Deploying commit: ${COMMIT_SHA:0:8}"
          echo "  Message: $(echo "$COMMIT_MSG" | head -1)"
          echo "  Strategy: Pre-pull → selective recreate (DB+nginx stay running)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          SSH_OPTS="-o ConnectTimeout=15 -o ServerAliveInterval=10 -o ServerAliveCountMax=3 -o StrictHostKeyChecking=no"

          ssh $SSH_OPTS "$GCE_USER@$GCE_HOST" \
            "GITHUB_REPOSITORY='$GITHUB_REPOSITORY' COMMIT_SHA='$COMMIT_SHA' GHCR_TOKEN='$GHCR_TOKEN' GH_ACTOR='$GH_ACTOR' IMAGE_PREFIX='$IMAGE_PREFIX' bash -s" << 'ENDSSH'
            set -euo pipefail

            APP_DIR="/opt/itrade/app"
            COMPOSE_FILE="$APP_DIR/docker-compose.prod.yml"
            NGINX_CONF="/opt/itrade/nginx.conf"
            CERTBOT_ENV="/opt/itrade/.env.certbot"
            CERTS_BASE="/opt/itrade/certbot/certs"
            WEBROOT_DIR="/opt/itrade/certbot/webroot"
            DEPLOY_START=$(date +%s)

            # ══════════════════════════════════════════════════════
            # Phase 1: Preparation (old containers still serving traffic)
            # ══════════════════════════════════════════════════════
            echo ""
            echo "══ Phase 1: Preparation (zero downtime) ══"

            download_from_github() {
              local source_path="$1"
              local dest_path="$2"
              local tmp_path="${dest_path}.tmp"
              local ok=false

              if curl -fsSL --connect-timeout 10 --max-time 30 \
                "https://raw.githubusercontent.com/$GITHUB_REPOSITORY/$COMMIT_SHA/$source_path" \
                -o "$tmp_path"; then
                ok=true
              elif curl -fsSL --connect-timeout 10 --max-time 30 \
                -H "Accept: application/vnd.github.raw" \
                "https://api.github.com/repos/$GITHUB_REPOSITORY/contents/$source_path?ref=$COMMIT_SHA" \
                -o "$tmp_path"; then
                ok=true
              fi

              if [ "$ok" = true ] && [ -s "$tmp_path" ]; then
                mv "$tmp_path" "$dest_path"
                echo "  ✅ Downloaded: $source_path"
                return 0
              fi

              rm -f "$tmp_path"
              echo "  ❌ Failed to download: $source_path"
              return 1
            }

            echo "▶ Downloading deployment files..."
            mkdir -p "$APP_DIR"
            cd "$APP_DIR"
            download_from_github "docker-compose.prod.yml" "$COMPOSE_FILE"

            if [ ! -s "$COMPOSE_FILE" ] || ! grep -q "services:" "$COMPOSE_FILE"; then
              echo "❌ docker-compose.prod.yml is empty or invalid"
              exit 1
            fi
            FILE_SIZE=$(wc -c < "$COMPOSE_FILE")
            echo "  ✅ docker-compose.prod.yml valid (${FILE_SIZE} bytes)"

            echo "▶ Verifying environment files..."
            sudo chown -R $(whoami):$(whoami) /opt/itrade/ 2>/dev/null || true
            sudo chmod 755 /opt/itrade/ /opt/itrade/app/ 2>/dev/null || true
            sudo chmod 600 /opt/itrade/.env.* 2>/dev/null || true

            for env_file in /opt/itrade/.env.{console,db,web,certbot}; do
              if [ ! -f "$env_file" ]; then
                echo "❌ Missing: $env_file"
                exit 1
              fi
            done
            echo "  ✅ All environment files present"

            echo "▶ Preparing nginx config and SSL paths..."
            DOMAIN=$(sed -n 's/^DOMAIN=//p' "$CERTBOT_ENV" | head -n 1 | tr -d '[:space:]"')
            if [ -z "${DOMAIN:-}" ]; then
              echo "❌ DOMAIN is missing in $CERTBOT_ENV"
              exit 1
            fi

            if [ -d "$NGINX_CONF" ]; then
              mv "$NGINX_CONF" "/opt/itrade/nginx.conf.backup.$(date +%s)"
            fi

            mkdir -p "$CERTS_BASE/live/$DOMAIN" "$WEBROOT_DIR"
            {
              echo 'server {'
              echo '    listen 80;'
              echo '    listen [::]:80;'
              echo "    server_name ${DOMAIN};"
              echo ''
              echo '    location /.well-known/acme-challenge/ {'
              echo '        root /var/www/certbot;'
              echo '    }'
              echo ''
              echo '    location / {'
              echo '        return 301 https://$host$request_uri;'
              echo '    }'
              echo '}'
              echo ''
              echo 'server {'
              echo '    listen 443 ssl;'
              echo '    listen [::]:443 ssl;'
              echo '    http2 on;'
              echo "    server_name ${DOMAIN};"
              echo ''
              echo "    ssl_certificate     /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;"
              echo "    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;"
              echo ''
              echo '    ssl_protocols       TLSv1.2 TLSv1.3;'
              echo '    ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;'
              echo '    ssl_prefer_server_ciphers off;'
              echo '    ssl_session_cache   shared:SSL:10m;'
              echo '    ssl_session_timeout 1d;'
              echo '    ssl_session_tickets off;'
              echo ''
              echo '    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;'
              echo '    add_header X-Content-Type-Options    nosniff;'
              echo '    add_header X-Frame-Options           SAMEORIGIN;'
              echo '    add_header Referrer-Policy           "strict-origin-when-cross-origin";'
              echo ''
              echo '    client_max_body_size 10M;'
              echo ''
              echo '    location / {'
              echo '        resolver 127.0.0.11 ipv6=off valid=30s;'
              echo '        set $web_upstream web;'
              echo '        proxy_pass         http://$web_upstream:3002;'
              echo '        proxy_http_version 1.1;'
              echo '        proxy_set_header Upgrade    $http_upgrade;'
              echo '        proxy_set_header Connection "upgrade";'
              echo '        proxy_set_header Host              $host;'
              echo '        proxy_set_header X-Real-IP         $remote_addr;'
              echo '        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;'
              echo '        proxy_set_header X-Forwarded-Proto $scheme;'
              echo '        proxy_cache_bypass $http_upgrade;'
              echo '        proxy_connect_timeout 60s;'
              echo '        proxy_send_timeout    60s;'
              echo '        proxy_read_timeout    60s;'
              echo '        proxy_next_upstream error timeout http_502 http_503;'
              echo '        proxy_next_upstream_tries    2;'
              echo '        proxy_next_upstream_timeout  30s;'
              echo '    }'
              echo '}'
            } > "$NGINX_CONF"
            chmod 644 "$NGINX_CONF"

            DUMMY_CERT="$CERTS_BASE/live/$DOMAIN/fullchain.pem"
            DUMMY_KEY="$CERTS_BASE/live/$DOMAIN/privkey.pem"
            if [ ! -s "$DUMMY_CERT" ] || [ ! -s "$DUMMY_KEY" ]; then
              echo "  ▶ Generating temporary bootstrap certificate..."
              openssl req -x509 -nodes -newkey rsa:2048 -days 1 \
                -keyout "$DUMMY_KEY" \
                -out "$DUMMY_CERT" \
                -subj "/CN=$DOMAIN" >/dev/null 2>&1
              echo "  ✅ Bootstrap certificate generated"
            else
              echo "  ✅ Existing certificate found"
            fi

            # Disable host nginx if running (only needed on first deploy)
            if command -v systemctl >/dev/null 2>&1 && systemctl is-active --quiet nginx; then
              echo "  ⚠️  Stopping host nginx to avoid port conflict"
              sudo systemctl stop nginx || true
              sudo systemctl disable nginx || true
            fi

            echo "▶ Logging in to GHCR..."
            echo "$GHCR_TOKEN" | docker login ghcr.io -u "$GH_ACTOR" --password-stdin

            export CONSOLE_IMAGE="$IMAGE_PREFIX/console:$COMMIT_SHA"
            export SCHEMA_MIGRATOR_IMAGE="$IMAGE_PREFIX/schema-migrator:$COMMIT_SHA"
            export WEB_IMAGE="$IMAGE_PREFIX/web:$COMMIT_SHA"

            echo "▶ Pre-pulling new images (services still running)..."
            PULL_START=$(date +%s)

            docker compose -f "$COMPOSE_FILE" pull console schema-migrator web

            PULL_END=$(date +%s)
            echo "  ✅ Images pre-pulled in $((PULL_END - PULL_START))s (zero downtime)"

            PHASE1_END=$(date +%s)
            echo "══ Phase 1 complete: $((PHASE1_END - DEPLOY_START))s (zero downtime) ══"

            # ══════════════════════════════════════════════════════
            # Phase 2: Rolling update (minimal downtime)
            # Only recreate services with new images.
            # DB and nginx stay running throughout.
            # ══════════════════════════════════════════════════════
            echo ""
            echo "══ Phase 2: Rolling update (minimal downtime starts) ══"
            DOWNTIME_START=$(date +%s)

            # Ensure DB is running and healthy (noop if already running)
            docker compose -f "$COMPOSE_FILE" up -d --no-build db
            echo "▶ Waiting for database..."
            DB_TIMEOUT=30
            DB_ELAPSED=0
            while [ $DB_ELAPSED -lt $DB_TIMEOUT ]; do
              if docker inspect --format='{{.State.Health.Status}}' itrade-db 2>/dev/null | grep -q "healthy"; then
                break
              fi
              sleep 2
              DB_ELAPSED=$((DB_ELAPSED + 2))
            done
            if [ $DB_ELAPSED -ge $DB_TIMEOUT ]; then
              echo "❌ Database health check timeout"
              docker compose -f "$COMPOSE_FILE" logs --tail=20 db
              exit 1
            fi
            echo "  ✅ Database healthy (waited ${DB_ELAPSED}s)"

            # Run schema migrator with new image
            docker compose -f "$COMPOSE_FILE" rm -sf schema-migrator 2>/dev/null || true
            echo "▶ Running schema migration..."
            MIGRATION_START=$(date +%s)
            docker compose -f "$COMPOSE_FILE" up --no-build schema-migrator

            MIGRATOR_EXIT=$(docker inspect --format='{{.State.ExitCode}}' itrade-schema-migrator 2>/dev/null || echo "1")
            MIGRATION_END=$(date +%s)
            if [ "$MIGRATOR_EXIT" != "0" ]; then
              echo "❌ Schema migration failed (exit code: $MIGRATOR_EXIT)"
              docker compose -f "$COMPOSE_FILE" logs --tail=30 schema-migrator
              exit 1
            fi
            echo "  ✅ Schema migration complete ($((MIGRATION_END - MIGRATION_START))s)"

            # Recreate console and web with new images
            # docker compose up -d detects image changes and only recreates affected containers
            echo "▶ Updating console and web services..."
            docker compose -f "$COMPOSE_FILE" up -d --no-build console web

            # Ensure nginx is running and reload config
            docker compose -f "$COMPOSE_FILE" up -d --no-build nginx
            docker exec itrade-nginx nginx -s reload 2>/dev/null || true

            # Wait for web health
            echo "▶ Waiting for web service to be healthy..."
            WEB_TIMEOUT=90
            WEB_ELAPSED=0
            while [ $WEB_ELAPSED -lt $WEB_TIMEOUT ]; do
              if docker inspect --format='{{.State.Health.Status}}' itrade-web 2>/dev/null | grep -q "healthy"; then
                break
              fi
              sleep 3
              WEB_ELAPSED=$((WEB_ELAPSED + 3))
              if [ $((WEB_ELAPSED % 15)) -eq 0 ]; then
                echo "  Waiting... ${WEB_ELAPSED}s/${WEB_TIMEOUT}s"
              fi
            done

            DOWNTIME_END=$(date +%s)

            if [ $WEB_ELAPSED -ge $WEB_TIMEOUT ]; then
              echo "❌ Web service health check timeout"
              docker compose -f "$COMPOSE_FILE" logs --tail=50 web
              exit 1
            fi

            echo "  ✅ Web service healthy (waited ${WEB_ELAPSED}s)"
            echo "══ Phase 2 complete: ~$((DOWNTIME_END - DOWNTIME_START))s service downtime ══"

            # ══════════════════════════════════════════════════════
            # Phase 3: Post-deploy (services are already healthy)
            # ══════════════════════════════════════════════════════
            echo ""
            echo "══ Phase 3: Post-deploy tasks (zero downtime) ══"

            # Conditional certbot: only run if certificate needs attention
            CERT_FILE="$CERTS_BASE/live/$DOMAIN/fullchain.pem"
            NEED_CERT=false

            if [ ! -s "$CERT_FILE" ]; then
              NEED_CERT=true
              echo "  Certificate missing → will issue"
            elif openssl x509 -in "$CERT_FILE" -noout -subject 2>/dev/null | grep -qi "O = \|self.signed\|CN = $DOMAIN" && \
                 ! openssl x509 -in "$CERT_FILE" -noout -issuer 2>/dev/null | grep -qi "Let's Encrypt\|R3\|R10\|R11\|E1\|E5"; then
              NEED_CERT=true
              echo "  Self-signed certificate detected → will issue real one"
            elif ! openssl x509 -in "$CERT_FILE" -noout -checkend 2592000 2>/dev/null; then
              NEED_CERT=true
              echo "  Certificate expires within 30 days → will renew"
            fi

            if [ "$NEED_CERT" = true ]; then
              CERTBOT_EMAIL=$(sed -n 's/^CERTBOT_EMAIL=//p' "$CERTBOT_ENV" | head -n 1 | tr -d '[:space:]"')
              if [ -n "${CERTBOT_EMAIL:-}" ]; then
                # Certbot refuses to overwrite a live directory it didn't create
                # (e.g. our bootstrap self-signed cert). Clean it up so certbot
                # can manage the directory from scratch.
                CERT_LIVE_DIR="$CERTS_BASE/live/$DOMAIN"
                CERT_RENEWAL_CONF="$CERTS_BASE/renewal/$DOMAIN.conf"
                if [ -d "$CERT_LIVE_DIR" ] && [ ! -f "$CERT_RENEWAL_CONF" ]; then
                  echo "  ▶ Clearing non-certbot certificate from live directory..."
                  rm -rf "$CERT_LIVE_DIR"
                  rm -rf "$CERTS_BASE/archive/$DOMAIN" 2>/dev/null || true
                fi

                echo "▶ Issuing/renewing Let's Encrypt certificate..."
                if docker compose -f "$COMPOSE_FILE" run --rm --entrypoint certbot certbot certonly \
                  --webroot \
                  --webroot-path=/var/www/certbot \
                  --domain "$DOMAIN" \
                  --email "$CERTBOT_EMAIL" \
                  --agree-tos \
                  --non-interactive \
                  --keep-until-expiring \
                  --cert-name "$DOMAIN"; then
                  docker exec itrade-nginx nginx -s reload
                  echo "  ✅ Certificate issued/renewed and nginx reloaded"
                else
                  # Certbot failed — restore bootstrap cert so nginx can still reload
                  if [ ! -s "$CERTS_BASE/live/$DOMAIN/fullchain.pem" ]; then
                    echo "  ▶ Restoring bootstrap certificate..."
                    mkdir -p "$CERTS_BASE/live/$DOMAIN"
                    openssl req -x509 -nodes -newkey rsa:2048 -days 1 \
                      -keyout "$CERTS_BASE/live/$DOMAIN/privkey.pem" \
                      -out "$CERTS_BASE/live/$DOMAIN/fullchain.pem" \
                      -subj "/CN=$DOMAIN" >/dev/null 2>&1
                  fi
                  echo "  ⚠️  Certificate issuance failed (non-fatal, services running with self-signed cert)"
                fi
              fi
            else
              echo "  ✅ Certificate is valid, skipping certbot"
            fi

            # Start certbot renewal service
            docker compose -f "$COMPOSE_FILE" up -d --no-build certbot

            # Ensure nginx reload cron for certificate renewals
            CRON_JOB="0 */12 * * * docker exec itrade-nginx nginx -s reload >> /var/log/nginx-reload.log 2>&1"
            if ! crontab -l 2>/dev/null | grep -q "itrade-nginx nginx -s reload"; then
              (crontab -l 2>/dev/null; echo "# iTrade nginx SSL reload"; echo "$CRON_JOB") | crontab -
              echo "  ✅ Installed nginx reload cron"
            fi

            docker logout ghcr.io >/dev/null 2>&1 || true

            DEPLOY_END=$(date +%s)

            # ── Final status ──
            echo ""
            echo "▶ Final container status:"
            docker compose -f "$COMPOSE_FILE" ps

            echo ""
            echo "🎉 Deployment successful!"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "  Total pipeline time:  $((DEPLOY_END - DEPLOY_START))s"
            echo "  Image pre-pull:       $((PULL_END - PULL_START))s (zero downtime)"
            echo "  Service downtime:     ~$((DOWNTIME_END - DOWNTIME_START))s"
            echo "  Schema migration:     $((MIGRATION_END - MIGRATION_START))s"
            echo "  Web health wait:      ${WEB_ELAPSED}s"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH

      - name: Report deployment status
        if: always()
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            echo "✅ Deployment succeeded: ${{ github.sha }}"
          else
            echo "❌ Deployment failed: ${{ github.sha }}"
            exit 1
          fi
